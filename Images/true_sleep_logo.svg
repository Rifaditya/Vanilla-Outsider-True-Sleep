<svg width="600" height="200" viewBox="0 0 600 200" xmlns="http://www.w3.org/2000/svg">
  <!-- Styles -->
  <style>
    .subtitle {
      fill: #ffffff;
      font-family: 'Segoe UI', sans-serif;
      font-size: 24px;
      font-weight: 300;
      letter-spacing: 2px;
      text-anchor: middle;
      filter: drop-shadow(0px 2px 2px rgba(0,0,0,0.5));
    }
    .title {
      fill: #ffffff;
      font-family: 'Segoe UI', sans-serif;
      font-size: 82px;
      font-weight: 700;
      letter-spacing: -2px;
      text-anchor: middle;
      text-transform: uppercase;
      filter: drop-shadow(0px 4px 4px rgba(0,0,0,0.8));
    }
  </style>

  <!-- Subtitle -->
  <text x="300" y="70" class="subtitle">Vanilla Outsider</text>

  <!-- Title -->
  <text x="300" y="150" class="title">TRUE SLEEP</text>
</svg>
